import React from 'react';
import logo from './image.png';

const NavBar = () => (
  <header className="App-header">
    <img src={logo} alt="Logo" className="covid-logo" />
  </header>
);

export default NavBar;
